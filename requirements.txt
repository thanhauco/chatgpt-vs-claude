# Core data + config
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0

# Dashboard
streamlit>=1.30
plotly>=5.18

# Optional: accurate token counting (falls back to a heuristic if missing)
tiktoken>=0.5

# Optional: real model providers (only needed for live runs)
openai>=1.0
anthropic>=0.25

# Dev
pytest>=7.0
